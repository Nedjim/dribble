import React from 'react';

export default class Date extends React.Component {
    render() {
       // typeof this.props.date;
        console.log(this.props.date);
        return (
            <div >
            </div>
        );
    }
}